#!/command/with-contenv sh
# shellcheck shell=sh
exec 2>&1
# Run from the service directory so s6-notifyoncheck finds data/check.
# Readiness delays nginx startup until FPM accepts connections.
exec s6-notifyoncheck -d php-fpm -F

prefix_rule(
    pattern = ["make"],
    decision = "allow",
)

prefix_rule(
    pattern = ["docker"],
    decision = "allow",
)

prefix_rule(
    pattern = ["docker-compose"],
    decision = "allow",
)

prefix_rule(
    pattern = ["dgoss"],
    decision = "allow",
)

prefix_rule(
    pattern = ["gh"],
    decision = "allow",
)

prefix_rule(
    pattern = ["git"],
    decision = "allow",
)
